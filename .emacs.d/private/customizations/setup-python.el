;; format on save with YAPF
;; (setq python-enable-yapf-format-on-save t)
(setq python-backend 'lsp)
(setq python-formatter 'yapf)
(setq python-format-on-save 't)
(setq python-save-before-test nil)
(setq python-sort-imports-on-save t)
(setq python-test-runner 'pytest)
